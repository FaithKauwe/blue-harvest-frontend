import "./dashboard.css";
import InputData from "../inputDataPage/inputData";

const Dashboard = ({}) => {
  return (
    <section>
      <InputData></InputData>
      <button></button>
    </section>
  );
};

export default Dashboard;
